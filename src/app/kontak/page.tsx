"use client";
import PublicSiteShell from "@/components/PublicSiteShell";
import { useWebsiteSettings } from "@/lib/site-settings";
export default function KontakPage() { const { settings } = useWebsiteSettings(); const mapUrl = settings.mapsEmbedUrl || ""; return <PublicSiteShell><main className="container page-section"><span className="eyebrow">Kontak Desa</span><h1>Kami siap membantu</h1><div className="contact-grid"><article className="content-card"><h2>{settings.namaWebsite}</h2>{settings.alamat && <p><i className="fa-solid fa-location-dot" /> {settings.alamat}</p>}{settings.kontak && <p><i className="fa-solid fa-phone" /> {settings.kontak}</p>}{settings.whatsapp && <p><i className="fa-brands fa-whatsapp" /> <a href={`https://wa.me/${settings.whatsapp.replace(/\D/g, "")}`}>WhatsApp Desa</a></p>}{settings.email && <p><i className="fa-solid fa-envelope" /> {settings.email}</p>}{settings.officeHours && <p><i className="fa-solid fa-clock" /> {settings.officeHours}</p>}</article><article className="map-placeholder">{mapUrl ? <iframe title={`Peta ${settings.namaWebsite}`} className="map-frame" loading="lazy" src={mapUrl} /> : <><i className="fa-solid fa-map-location-dot" /><h2>Lokasi belum diatur</h2><p>Admin dapat menambahkan Google Maps Embed URL dari Pengaturan Website.</p></>}<div className="social-links detail-links">{settings.socials.map((social) => <a href={social.url} target="_blank" rel="noreferrer" key={social.label} aria-label={social.label}><i className={`fa-brands ${social.icon}`} /></a>)}</div></article></div></main></PublicSiteShell>; }
