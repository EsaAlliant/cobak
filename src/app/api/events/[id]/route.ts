import { deleteCollection, updateCollection } from "@/lib/cms-api"; export const PATCH = (request: Request, { params }: { params: Promise<{ id: string }> }) => params.then(({ id }) => updateCollection(request, "events", id, ["admin"])); export const DELETE = (_request: Request, { params }: { params: Promise<{ id: string }> }) => params.then(({ id }) => deleteCollection("events", id, ["admin"]));
