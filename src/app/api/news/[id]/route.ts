import { deleteCollection, updateCollection } from "@/lib/cms-api"; export const PATCH = (request: Request, { params }: { params: Promise<{ id: string }> }) => params.then(({ id }) => updateCollection(request, "news", id, ["admin"])); export const DELETE = (_request: Request, { params }: { params: Promise<{ id: string }> }) => params.then(({ id }) => deleteCollection("news", id, ["admin"]));
