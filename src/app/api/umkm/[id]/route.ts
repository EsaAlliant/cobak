import { deleteCollection, updateCollection } from "@/lib/cms-api"; export const PATCH = (request: Request, { params }: { params: Promise<{ id: string }> }) => params.then(({ id }) => updateCollection(request, "umkm", id, ["admin", "operator"])); export const DELETE = (_request: Request, { params }: { params: Promise<{ id: string }> }) => params.then(({ id }) => deleteCollection("umkm", id, ["admin", "operator"]));
