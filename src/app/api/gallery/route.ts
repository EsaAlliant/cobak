import { createCollection, listCollection } from "@/lib/cms-api"; export const GET = () => listCollection("gallery"); export const POST = (request: Request) => createCollection(request, "gallery", ["admin"]);
