"use client";
import { useEffect, useState } from "react";
import Swal from "sweetalert2";
import { useForm } from "react-hook-form";
import { profileSchema } from "@/lib/validators/users";
type Form = { name: string; email: string; phone: string; password: string; avatar_url: string };
export default function ProfileForm() { const { register, handleSubmit, reset, formState: { isSubmitting } } = useForm<Form>(); const [error, setError] = useState(""); const [avatar, setAvatar] = useState<File | null>(null); useEffect(() => { fetch("/api/users/me").then((r) => r.ok ? r.json() : null).then((body) => body?.data && reset({ ...body.data, password: "", phone: body.data.phone || "", avatar_url: body.data.avatar_url || "" })).catch(() => setError("Gagal memuat profil.")); }, [reset]); const submit = async (values: Form) => { const parsed = profileSchema.safeParse(values); if (!parsed.success) { setError(parsed.error.issues[0].message); return; } if (avatar) { const data = new FormData(); data.set("bucket", "branding"); data.set("file", avatar); const upload = await fetch("/api/upload", { method: "POST", body: data }); const body = await upload.json(); if (!upload.ok) { setError(body.error); return; } values.avatar_url = body.url; } const response = await fetch("/api/users/me", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); const body = await response.json(); if (!response.ok) { setError(body.error); return; } await Swal.fire({ icon: "success", title: "Profil diperbarui", timer: 1200, showConfirmButton: false }); reset({ ...values, password: "" }); }; return <form className="settings-form" onSubmit={handleSubmit(submit)}>{error && <p className="cms-error">{error}</p>}<label>Nama<input {...register("name")} /></label><label>Email<input type="email" {...register("email")} /></label><label>Telepon<input {...register("phone")} /></label><label>Password Baru <small>(opsional)</small><input type="password" {...register("password")} /></label><label>Foto Profil<input type="file" accept="image/*" onChange={(event) => setAvatar(event.target.files?.[0] || null)} /></label><button className="button primary" disabled={isSubmitting}>{isSubmitting ? "Menyimpan..." : "Simpan Profil"}</button></form>; }
